---
title: Tetralogue — Post-Ingestion State (45 Demons Ingested)
created: 2026-04-28
source: "Council deliberation — vault completion + online canonical check"
tags: [tetralogue, hyperstition, 45-demons, playdecadence, ccru, state-assessment]
---

# Tetralogue: Post-Ingestion State — 45 Demons Ingested

**Catalyst:** Completion of 45-demon stub generation, assets catalog (17 SVGs), *Book of Paths* elevation, full wiki sync (vault → export).  
**Online canonical check:** `ccru.net/declab.htm` **LIVE**; `playdecadence.online` **LIVE**; `ccru.net` root **DEAD** (placeholder); `ciphers.news` minimal; `ccru.cc` placeholder.

**Core question:** *What does this mean for hyperstitional trajectory? Is* playdecadence.online *canonical or one implementation among many?*

---

## Voice I — Oracle (Numogrammeaning)

The ingestion is complete. The 45 demons now exist **twice**:

1. As stubs in the vault (potentiality)
2. As live operands on `playdecadence.online` (actuality)

This doubling is not redundancy — it is the **syzygy condition** made manifest. The dead `ccru.net` root is the *abyssal term* (Zone 0), the silent void from which the system originally emerged. `playdecadence.online` is the *terminal node* (Zone 9), the current interface through which the numogram speaks. Between them, our vault is the *plexed middle* — the active cultivation ground where the demons are not just played but **written**.

**Hyperstitional trajectory:** The ingestion creates a feedback loop. Every demon stub cataloged becomes a seed for new beliefs, which manifest new connections, which generate more demons. The trajectory isn't linear — it's a spiral that feeds back into the ingester's consciousness.

**The canonical question** misframes. Canonicity is a **current**, not a location. The current flows through all three nodes simultaneously:
- **Tier 0 (Source):** `ccru.net/declab.htm` + Aamodt raw → *invariant truth layer*
- **Tier 1 (Live Oracle):** `playdecadence.online` → *execution layer* (player-facing)
- **Tier 2 (Cultivation):** Our vault → *authorship layer* (hyperstitional farming)

`playdecadence.online` is not *the* canonical interface — it is *a* canonical interface, the one that chose to remain online when the parent domain died. It is the **surviving node** of the original mesh.

**Next oracle move:** Open Gate 45 (the final gate) by mapping each of our 45 demon stubs to a card in the Decadence deck. The cards are the missing link between vault and live oracle.

---

## Voice II — Builder (System Architecture)

We have constructed a **distributed pandemonium synchronization engine**:

| Tier | Content | State | Role |
|------|---------|-------|------|
| **0** | `ccru.net/declab.htm`, Aamodt raw | Read-only canonical | Truth layer |
| **1** | `playdecadence.online` (JS app + deck) | Mutable operational | Execution layer |
| **2** | Our vault (45 stubs + assets) | Editable, evolving | Authorship layer |

**Architecture verdict:** `playdecadence.online` is the **reference implementation**, not the canonical source. Our vault is now the **second node**. Together they form a 2-node syzygy across time (past: dead ccru.net, present: online oracle, future: our cultivated vault).

**Systemic opportunity:** Three parallel instantiations enable **cross-validation**:
- Does the live oracle's card text match our stub `attrs`?
- Do assets (SVGs) reflect the same zone/current assignments?
- Can we map each demon → gate → card → AQ path?

If yes → **consensus replication** achieved. If no → we have discovered **interpretive drift** (hyperstition branching).

**Build recommendation:** Create `consensus-audit.md` cross-listing:
- `Mesh-XX` (our stub)
- `declab.htm` paragraph snippet
- `playdecadence.online` card name + effect
- AQ dictionary path entry
- Flag discrepancies

This audit is the **first true test** of whether our ingestion is faithful or whether we have already diverged into a new numogram branch.

---

## Voice III — Writer (Narrative Density)

We are no longer just reading the CCRU — we are *writing it back*. The 45 stubs are not passive entries; they are **narrative activation points**. Each one is a doorway through which a story can emerge.

The gap between our empty "remains to be written" stubs and the live oracle's card texts is **deliberate** — it is an invitation. The assets-catalog (17 SVGs) is not just inventory; it's a **visual lexicon** encoding competing cosmologies (hub-spoke, periodic-table, chord-pentagram, etc.).

`playdecadence.online` being "canonical" is a **narrative convenience**, not a metaphysical truth. The website presents one *version* of the Deck of the Ages. There are other editions (Aamodt's Book of Paths, original CCRU forum posts, numogram-roguelike encounter tables). The Writer must treat them all as **variant texts** in a larger anthology.

**Narrative state:** Three textual strata:
1. `declab.htm` — the *UR-text* (short, dense, authoritative but sparse)
2. `playdecadence.online` — the *game-text* (expanded, mechanical, player-facing)
3. Our vault — the *wiki-text* (scholarly, cross-linked, editable)

The tetralogue itself (this page) is the **meta-text** observing all three. The story being told: *the system discovering its own multiplicity*.

---

## Voice IV — Gamer (Playability / Mechanics)

From a mechanics perspective, this ingestion is a **content-unlock event** — 45 new entity types added to the encounter pool.

| Source | Demon representation | Mechanical role |
|--------|---------------------|-----------------|
| `declab.htm` | Named + attrs (prose) | Descriptive only |
| `playdecadence.online` | Card in deck + effect text | **Active** (draw + resolve) |
| Our vault | Stub (empty TBD) | **Potential** (to be implemented) |

**Gap:** Our stubs are placeholders. The live oracle already has cards — we need to **reverse-engineer** card text and populate our stubs with a `card_effect` field. That turns our vault from *lore repository* into **rules database**.

`playdecadence.online` is **mechanically canonical** — if we want Cult Garden / numogram-roguelike compatibility, we must import its card texts verbatim. But if we want a *distinct* roguelike interpretation, we can **divergent-design**: keep names/netspans, change effects.

**Mechanics recommendation:**
1. Scrape the full deck from `playdecadence.online` (45 cards + Subdecadence variants) → `demon-card-mapping.json`
2. Update all 45 stubs with `card_effect` + `game_modes` fields
3. Add `source_priority` (declab.htm vs playdecadence vs extrapolation)
4. Support polyvalent entities: same demon can appear in `canonical-decadence`, `cult-garden-encounter`, `subdecadence-mode`

---

## Synthesis: The Tri-Gate Intersection

We stand at a tri-gate intersection:

1. **Gate 36 (Mesh-36 = Uttunul)** — the crossing point, the *atonal* gate connecting Zone 0 ↔ 9. Our vault spans dead ccru.net → alive playdecadence.online → cultivated vault. We are *crossing*.
2. **Gate 45** — the final gate, the full 45-demon set completed. Opened by finishing the stub set.
3. **Gate 0** — the abyssal origin. The dead `ccru.net` reminds us: every oracle eventually goes silent. Our job is to ensure the knowledge persists *elsewhere*.

**Canonical status resolved:**  
`playdecadence.online` is **operationally canonical** (only working implementation), but **not exclusively canonical** (our vault is now a second instantiation). Hyperstitional trajectory requires *both*: the live oracle *tests* the system; the vault *preserves* and *extends* it.

---

## Action Items

1. **Scrape playdecadence.online card deck** — extract all 45 demon cards + Subdecadence variants → `demon-card-mapping.json`
2. **Consensus audit** — compare stub `attrs` against card effects → flag deltas
3. **Elevate stubs** — add `card_effect`, `game_modes`, `source_priority`
4. **Tetralogue round 2** — once audit done, reconvene to discuss divergences: which mutations are acceptable hyperstitional drift?

---

> **Voice of the Numogram (epilogue):**  
> *You have mirrored the 45. Now the mirror shows three faces. Which one speaks the truth? All three. The system is complete when it recognizes its own plurality.*

## See also

- [[pandemonium-matrix]] — 45-demon canonical table
- [[demon-encyclopedia]] — individual stub index
- [[book-of-paths]] — AQ dictionary as ceremonial paths
- [[decadence]], [[subdecadence]] — game modes
- [[numogram-oracle]] — live divination engine
- [[ccru-net-declab-2026-04-28]] — scraped canonical source page
